name: Deploy to Production

on:
  push:
    branches:
      - master

jobs:
  deploy:
    name: Build and Deploy to Production
    runs-on: ubuntu-latest
    environment:
      name: production

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build application
        env:
          VITE_DOCS_BASE_URL: ${{ secrets.VITE_DOCS_BASE_URL }}
          VITE_DONATEX_CLIENT_ID: ${{ secrets.VITE_DONATEX_CLIENT_ID }}
          VITE_ERROR_TRACKING_PROVIDER: ${{ secrets.VITE_ERROR_TRACKING_PROVIDER }}
          VITE_GLITCHTIP_DSN: ${{ secrets.VITE_GLITCHTIP_DSN }}
          VITE_GLITCHTIP_ENVIRONMENT: ${{ secrets.VITE_GLITCHTIP_ENVIRONMENT }}
          VITE_GOOGLE_ANALYTICS_MEASUREMENT_ID: ${{ secrets.VITE_GOOGLE_ANALYTICS_MEASUREMENT_ID }}
          VITE_KINOPOISK_METADATA_WORKER_URL: ${{ secrets.VITE_KINOPOISK_METADATA_WORKER_URL }}
          VITE_SENTRY_DSN: ${{ secrets.VITE_SENTRY_DSN }}
          VITE_SENTRY_ENVIRONMENT: ${{ secrets.VITE_SENTRY_ENVIRONMENT }}
          VITE_TMDB_API_KEY: ${{ secrets.VITE_TMDB_API_KEY }}
          VITE_TWITCH_CLIENT_ID: ${{ secrets.VITE_TWITCH_CLIENT_ID }}
        run: pnpm build:ssg

      - name: Generate source hashes manifest
        run: node scripts/generate-source-hashes.js

      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          SHORT_SHA=$(echo ${{ github.sha }} | cut -c1-7)
          TAG_NAME="deploy-${SHORT_SHA}"
          RELEASE_TITLE="Deployment - ${SHORT_SHA}"

          # Create release
          gh release create "${TAG_NAME}" \
            --title "${RELEASE_TITLE}" \
            --notes "Automated deployment from commit ${{ github.sha }}" \
            --target ${{ github.sha }}

      - name: Upload source hashes to release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          SHORT_SHA=$(echo ${{ github.sha }} | cut -c1-7)
          TAG_NAME="deploy-${SHORT_SHA}"

          # Upload the manifest file as a release asset
          gh release upload "${TAG_NAME}" dist/source-hashes.json \
            --clobber

      - name: Build documentation
        run: pnpm docs:build

      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -p ${{ secrets.SSH_PORT }} ${{ secrets.SSH_HOST }} >> ~/.ssh/known_hosts

      - name: Deploy application to production server
        run: |
          rsync -avz --delete \
            -e "ssh -i ~/.ssh/deploy_key -p ${{ secrets.SSH_PORT }} -o StrictHostKeyChecking=yes" \
            dist/ \
            ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }}:/var/www/pointauc.com/html/

      - name: Deploy documentation to production server
        run: |
          rsync -avz --delete \
            -e "ssh -i ~/.ssh/deploy_key -p ${{ secrets.SSH_PORT }} -o StrictHostKeyChecking=yes" \
            docs/.vitepress/dist/ \
            ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }}:/var/www/pointauc.com/html/docs/

      - name: Cleanup
        if: always()
        run: rm -f ~/.ssh/deploy_key
