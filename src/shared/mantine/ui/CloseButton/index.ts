import { CloseButton } from '@mantine/core';

import classes from '@shared/mantine/ui/CloseButton/index.module.css';

const CloseButtonExtended = CloseButton.extend({
  classNames: classes,
});

export default CloseButtonExtended;
