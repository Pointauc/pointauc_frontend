import React, { FC } from 'react';
import PageContainer from '../PageContainer/PageContainer';

const TbdPage: FC = () => {
  // eslint-disable-next-line @typescript-eslint/ban-ts-ignore
  // @ts-ignore
  return <PageContainer title="В разработке" />;
};

export default TbdPage;
